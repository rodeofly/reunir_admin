<%= form_for(@medecin) do |f| %>
  <% if @medecin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medecin.errors.count, "error") %> prohibited this medecin from being saved:</h2>

      <ul>
      <% @medecin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
