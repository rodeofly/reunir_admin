<%= form_for(@groupe_enfant) do |f| %>
  <% if @groupe_enfant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupe_enfant.errors.count, "error") %> prohibited this groupe_enfant from being saved:</h2>

      <ul>
      <% @groupe_enfant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
